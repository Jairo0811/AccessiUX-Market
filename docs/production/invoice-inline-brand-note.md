# Invoice inline brand note

The accessible invoice embeds the AccessiUX Market mark as inline SVG so browser printing/PDF generation does not depend on an external asset request at print time. The shared branded SVG and favicon remain versioned under `frontend/accessible-market-web/public/branding` for the application shell and fresh clones.
