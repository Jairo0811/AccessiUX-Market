# Orders feature

Protected Angular views for `/orders` and `/orders/:id`.

The feature intentionally treats backend cancellation metadata as authoritative. It does not derive eligibility from the browser clock.
