# AccessiUX Market branding

These assets are intentionally versioned so every fresh clone or deployment uses the same AccessiUX Market identity.

- `accessiux-mark.svg`: canonical compact brand mark used by documents and UI surfaces.
- `../favicon.svg`: browser/tab icon derived from the same mark.

Do not rely on untracked local files under `public/branding` for production branding.
